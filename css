body {
    text-align: center;
    font-family: cursive;
    transition: background 0.5s, color 0.5s;
}
.dark-mode {
    background: #222;
    color: white;
}
.toggle-btn {
    margin-top: 20px;
    padding: 10px 20px;
    font-size: 18px;
    cursor: pointer;
    border: none;
    border-radius: 5px;
    background: #007BFF;
    color: white;
}
.toggle-btn:hover {
    background: #0056b3;
}
